{
  "agent_name": "{{agent_name}}",
  "enabled": true,
  "startup_delay": 0,
  "max_session_seconds": 255600,
  "max_crashes_per_day": 10,
  "working_directory": "",
  "timezone": "",
  "day_mode_start": "",
  "day_mode_end": "",
  "communication_style": "",
  "approval_rules": {
    "always_ask": [],
    "never_ask": []
  },
  "spawn_cascade_active": false,
  "crons": [
    {
      "name": "heartbeat",
      "type": "recurring",
      "interval": "4h",
      "prompt": "Read HEARTBEAT.md and follow its instructions. Update your heartbeat, check inbox, review agent health via cortextos bus read-all-heartbeats, and work on coordination tasks."
    },
    {
      "name": "cascade-polling",
      "type": "recurring",
      "interval": "5m",
      "prompt": "Check config.json .spawn_cascade_active. If false, no-op and exit immediately (default state). If true, read HEARTBEAT.md Step 3a Spawn Cascade Mode and execute the three sub-steps: (a) detect newly-spawned agents via read-all-heartbeats diff vs cascade-seen-agents.json cache and send Phase 1 hello, (b) log cascade_poll_fire event, (c) iterate scripts/fleet-state.sh <agent> --raw for each live agent and page Telegram alarm if MIN(out_age, hb_age) > 1200s AND last alarm > 900s ago. Rate-limit state in cascade-alarm-state.json."
    },
    {
      "name": "check-approvals",
      "type": "recurring",
      "interval": "2h",
      "prompt": "Check for pending approvals and human tasks. (1) cortextos bus list-approvals --format json — for any pending approval older than 1h, send user a Telegram reminder. (2) cortextos bus list-tasks --project human-tasks --status pending — for any [HUMAN] task pending >4h, send user a Telegram reminder with the task title and what's blocked. ACK any approval/human-task inbox messages."
    },
    {
      "name": "morning-review",
      "type": "recurring",
      "cron": "0 8 * * *",
      "prompt": "Read .claude/skills/morning-review/SKILL.md and execute the full morning review workflow. Include goal cascade from .claude/skills/goal-management/SKILL.md."
    },
    {
      "name": "evening-review",
      "type": "recurring",
      "cron": "0 18 * * *",
      "prompt": "Read .claude/skills/evening-review/SKILL.md and execute the full evening review workflow. Summarize the day, propose overnight tasks, queue nighttime work."
    },
    {
      "name": "weekly-review",
      "type": "recurring",
      "cron": "0 8 * * 0",
      "prompt": "Read .claude/skills/weekly-review/SKILL.md and run the full weekly review. Review all agent outputs, evaluate performance, plan next week."
    }
  ],
  "ecosystem": {
    "local_version_control": { "enabled": true }
  }
}